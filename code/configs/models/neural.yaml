# Neural Baseline (Transducer) Configuration
# Uses SIGMORPHON 2023 task0-trm.sh script with predefined parameters
model_type: neural
task: forward  # Morphological inflection
data_source: unimorph  # Uses UniMorph data (no context)
output_dir_template: "results/neural-{lang}"

# Note: Training parameters are handled by the SIGMORPHON 2023 shell script
# (baselines/neural/neural-transducer-master/example/sigmorphon2023-shared-tasks/task0-trm.sh)
# No configuration needed here - the script handles everything
